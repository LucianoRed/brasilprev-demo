apiVersion: v1
kind: Service
metadata:
  name: brasilprev-service
  namespace: brasilprev
spec:
  selector:
    app: brasilprev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080 # Mesma porta do container